namespace IdentityServerHost.EasySso;

/// <summary>
/// It should be an interface with a class behind, but for a real code I hope you don't need it at all.
/// </summary>
public class IServerConfiguration
{
	public bool IsLocalEnvironment { get; set; }
}
